server:
  port: 8081

spring:
  application:
    name: account-service

  datasource:
    url: jdbc:mysql://localhost:3306/accounts_db
    username: root
    password: Tekejob$12%@

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  config:
    import: classpath:common-config.yaml

